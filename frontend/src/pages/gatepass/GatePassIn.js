import React from 'react';
import GatePassList from './GatePassList';

/** Gate Pass In — logistic and customer entries (page `gatepass_in`). */
export default function GatePassIn() {
  return <GatePassList direction="in" />;
}
