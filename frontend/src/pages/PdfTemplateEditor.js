import React, { useEffect, useMemo, useRef, useState } from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import { ArrowLeft, Save, Type, Square, Minus, Trash2, Copy, Plus, Search, ChevronDown, Image, FilePlus2, Link, QrCode } from 'lucide-react';
import QRCode from 'qrcode';
import toast from 'react-hot-toast';
import { pdfManagementAPI } from '../services/api';
import '../styles/pdfManagement.css';

const FORMATS={A4:{width:794,height:1123},A3:{width:1123,height:1587},A5:{width:559,height:794},LETTER:{width:816,height:1056},LEGAL:{width:816,height:1344},CUSTOM:{width:800,height:1000}};
const uid=()=>Math.random().toString(36).slice(2,10);
const cssObject=(css=[])=>Object.fromEntries((Array.isArray(css)?css:[]).filter(Boolean).map(x=>[x.property,x.value]));
const cssArray=obj=>Object.entries(obj).map(([property,value])=>({property,value:String(value)}));
const sample={document:{title:'QUOTATION',number:'QT-2026-0001',status:'draft',totalAmount:'PKR 5,250,000',taxAmount:'PKR 250,000',createdAt:'15/07/2026'},customer:{fullName:'Muhammad Ahmed',email:'customer@example.com',phone:'0300-0000000'}};
const resolve=text=>String(text||'').replace(/\{\{\s*([^}]+)\s*\}\}/g,(_,key)=>key.trim().split('.').reduce((v,p)=>v?.[p],sample)??`{{${key}}}`);
const newPage=()=>({config:{format:'A4',width:794,height:1123,backgroundColor:'#ffffff'},backgroundImage:'',bgSize:'cover',bgPosition:'center center',elements:[]});
const normalizePage=p=>({...newPage(),...p,config:{...newPage().config,...(p?.config||{})},elements:Array.isArray(p?.elements)?p.elements.map(e=>({...e,id:e.id||uid(),css:Array.isArray(e.css)?e.css:[]})):[]});
function QrPreview({value}){const [src,setSrc]=useState('');useEffect(()=>{QRCode.toDataURL(resolve(value)||'https://example.com',{margin:1,width:300}).then(setSrc).catch(()=>setSrc(''))},[value]);return src?<img src={src} alt="QR code"/>:null}

export default function PdfTemplateEditor(){
 const {id}=useParams(),navigate=useNavigate(),fileRef=useRef(null);
 const [template,setTemplate]=useState(null),[pageIndex,setPageIndex]=useState(0),[selectedId,setSelectedId]=useState(null),[variables,setVariables]=useState([]),[saving,setSaving]=useState(false),[variableOpen,setVariableOpen]=useState(false),[variableSearch,setVariableSearch]=useState(''),[elementOpen,setElementOpen]=useState(true);
 useEffect(()=>{let live=true;pdfManagementAPI.getTemplate(id).then(r=>{const t=r.data?.data?.template;if(!t)throw new Error();const pages=(t.designData?.pages||[]).map(normalizePage);const fixed={...t,designData:{...(t.designData||{}),pages:pages.length?pages:[newPage()]}};if(live)setTemplate(fixed);return pdfManagementAPI.getVariables(t.documentType)}).then(r=>{if(live)setVariables(r.data?.data?.variables||[])}).catch(()=>toast.error('Failed to load template'));return()=>{live=false}},[id]);
 const pages=template?.designData?.pages||[],page=pages[pageIndex]||null,selected=page?.elements?.find(e=>e.id===selectedId)||null,style=cssObject(selected?.css);
 const mutatePage=(index,fn)=>setTemplate(t=>{if(!t)return t;const next=structuredClone(t),target=next.designData.pages[index];if(!target)return t;fn(target);return next});
 const mutateElement=(elementId,fn,index=pageIndex)=>mutatePage(index,p=>{const el=p.elements.find(x=>x.id===elementId);if(el)fn(el)});
 const addElement=(type,text='',position={})=>{const el={id:uid(),type,text:type==='link'?'Open link':text,imageUrl:'',url:type==='link'?'https://example.com':'',qrValue:type==='qr'?'https://example.com':'',css:cssArray({left:`${position.x??60}px`,top:`${position.y??80}px`,width:type==='line'?'670px':type==='rectangle'?'240px':type==='qr'?'140px':type==='image'?'220px':'300px',height:type==='rectangle'?'120px':type==='line'?'1px':type==='qr'?'140px':type==='image'?'160px':'44px','font-size':'16px','font-weight':'400',color:type==='link'?'#2563eb':'#111827','background-color':type==='rectangle'?'#e2e8f0':'transparent','text-align':'left','border-radius':'0px',opacity:'1'})};mutatePage(pageIndex,p=>p.elements.push(el));setSelectedId(el.id);};
 const setStyle=(property,value)=>selectedId&&mutateElement(selectedId,el=>{const obj=cssObject(el.css);obj[property]=value;el.css=cssArray(obj)});
 const setText=text=>selectedId&&mutateElement(selectedId,el=>{el.text=text});
 const setField=(key,value)=>selectedId&&mutateElement(selectedId,el=>{el[key]=value});
 const removeElement=()=>{if(!selectedId)return;mutatePage(pageIndex,p=>{p.elements=p.elements.filter(e=>e.id!==selectedId)});setSelectedId(null)};
 const beginMove=(ev,el)=>{if(ev.button!==0)return;ev.preventDefault();setSelectedId(el.id);const s=cssObject(el.css),sx=ev.clientX,sy=ev.clientY,left=parseFloat(s.left)||0,top=parseFloat(s.top)||0,elementId=el.id,index=pageIndex;const move=e=>mutateElement(elementId,target=>{const o=cssObject(target.css);o.left=`${Math.max(0,left+e.clientX-sx)}px`;o.top=`${Math.max(0,top+e.clientY-sy)}px`;target.css=cssArray(o)},index);const up=()=>{window.removeEventListener('mousemove',move);window.removeEventListener('mouseup',up)};window.addEventListener('mousemove',move);window.addEventListener('mouseup',up)};
 const save=async()=>{setSaving(true);try{await pdfManagementAPI.updateTemplate(id,{name:template.name,status:template.status,description:template.description,designData:template.designData});toast.success('Template saved')}catch{toast.error('Save failed')}finally{setSaving(false)}};
 const addPage=()=>{setTemplate(t=>({...t,designData:{...t.designData,pages:[...t.designData.pages,newPage()]}}));setPageIndex(pages.length);setSelectedId(null)};
 const duplicatePage=()=>{const copy=structuredClone(page);copy.elements=copy.elements.map(e=>({...e,id:uid()}));setTemplate(t=>({...t,designData:{...t.designData,pages:[...t.designData.pages.slice(0,pageIndex+1),copy,...t.designData.pages.slice(pageIndex+1)]}}));setPageIndex(pageIndex+1);setSelectedId(null)};
 const deletePage=()=>{if(pages.length===1)return toast.error('At least one page is required');setTemplate(t=>({...t,designData:{...t.designData,pages:t.designData.pages.filter((_,i)=>i!==pageIndex)}}));setPageIndex(Math.max(0,pageIndex-1));setSelectedId(null)};
 const updatePageConfig=(key,value)=>mutatePage(pageIndex,p=>{p.config[key]=value});
 const setFormat=format=>{const size=FORMATS[format];mutatePage(pageIndex,p=>{p.config={...p.config,format,width:size.width,height:size.height}})};
 const uploadBackground=e=>{const file=e.target.files?.[0];if(!file)return;const reader=new FileReader();reader.onload=()=>mutatePage(pageIndex,p=>{p.backgroundImage=reader.result});reader.readAsDataURL(file);e.target.value=''};
 const uploadElementImage=e=>{const file=e.target.files?.[0];if(!file)return;const reader=new FileReader();reader.onload=()=>setField('imageUrl',reader.result);reader.readAsDataURL(file);e.target.value=''};
 const insertVariable=v=>{if(selected?.type==='text')setText(`${selected.text||''}${v.reference}`);else if(selected?.type==='image')setField('imageUrl',`${selected.imageUrl||''}${v.reference}`);else if(selected?.type==='link')setField('url',selected.url==='https://example.com'?v.reference:`${selected.url||''}${v.reference}`);else if(selected?.type==='qr')setField('qrValue',selected.qrValue==='https://example.com'?v.reference:`${selected.qrValue||''}${v.reference}`);else addElement('text',v.reference);setVariableOpen(false);setVariableSearch('')};
 const dropVariable=e=>{e.preventDefault();const ref=e.dataTransfer.getData('application/pdf-variable');if(!ref)return;const rect=e.currentTarget.getBoundingClientRect();addElement('text',ref,{x:Math.round(e.clientX-rect.left),y:Math.round(e.clientY-rect.top)})};
 const filtered=useMemo(()=>variables.filter(v=>`${v.key} ${v.label||''}`.toLowerCase().includes(variableSearch.toLowerCase())),[variables,variableSearch]);
 if(!template||!page)return <div className="pdf-editor-loading">Loading editor...</div>;
 const canvasStyle={width:page.config.width,height:page.config.height,backgroundColor:page.config.backgroundColor||'#fff',backgroundImage:page.backgroundImage?`url(${page.backgroundImage})`:undefined,backgroundSize:page.bgSize||'cover',backgroundPosition:page.bgPosition||'center center',backgroundRepeat:'no-repeat'};
 return <div className="pdf-editor">
  <header className="pdf-editor-header"><button className="icon-btn" title="Back" onClick={()=>navigate('/pdf-management')}><ArrowLeft size={19}/></button><input value={template.name} onChange={e=>setTemplate({...template,name:e.target.value})}/><select value={template.status} onChange={e=>setTemplate({...template,status:e.target.value})}><option value="draft">Draft</option><option value="active">Active</option><option value="inactive">Inactive</option></select><button className="btn btn-primary" onClick={save} disabled={saving}><Save size={17}/>{saving?'Saving...':'Save'}</button></header>
  <aside className="pdf-tools"><button className="pdf-add-element-trigger" onClick={()=>setElementOpen(v=>!v)}><span><Plus size={17}/>Add Element</span><ChevronDown size={16}/></button>{elementOpen&&<div className="pdf-tool-grid"><button onClick={()=>addElement('text','Text block')}><Type size={18}/>Text</button><button onClick={()=>addElement('image')}><Image size={18}/>Image</button><button onClick={()=>addElement('link')}><Link size={18}/>Link</button><button onClick={()=>addElement('qr')}><QrCode size={18}/>QR code</button><button onClick={()=>addElement('rectangle')}><Square size={18}/>Shape</button><button onClick={()=>addElement('line')}><Minus size={18}/>Line</button></div>}<div className="pdf-variable-picker"><button className="pdf-variable-trigger" onClick={()=>setVariableOpen(v=>!v)}><span><Search size={16}/>Variables</span><ChevronDown size={16}/></button>{variableOpen&&<div className="pdf-variable-menu"><div className="pdf-variable-search"><Search size={15}/><input autoFocus placeholder="Search variables" value={variableSearch} onChange={e=>setVariableSearch(e.target.value)}/></div><div className="pdf-variable-results">{filtered.map(v=><button key={v.key} draggable onDragStart={e=>e.dataTransfer.setData('application/pdf-variable',v.reference)} onClick={()=>insertVariable(v)}><code>{v.reference}</code><small>{v.label||v.category}</small></button>)}{!filtered.length&&<p>No variables found</p>}</div></div>}</div><h3>Pages</h3><div className="pdf-page-list">{pages.map((_,i)=><button className={i===pageIndex?'active':''} onClick={()=>{setPageIndex(i);setSelectedId(null)}} key={i}><FilePlus2 size={15}/>Page {i+1}</button>)}</div><button onClick={addPage}><Plus size={17}/>Add page</button><button onClick={duplicatePage}><Copy size={17}/>Duplicate page</button><button className="danger" onClick={deletePage}><Trash2 size={17}/>Delete page</button></aside>
  <main className="pdf-canvas-area"><div className="pdf-page-meta">Page {pageIndex+1} of {pages.length} - {page.config.format}</div><div className="pdf-canvas" style={canvasStyle} onDragOver={e=>e.preventDefault()} onDrop={dropVariable} onMouseDown={e=>{if(e.target===e.currentTarget)setSelectedId(null)}}>{page.elements.map(el=>{const s=cssObject(el.css);return <div key={el.id} className={`pdf-element ${selectedId===el.id?'selected':''} ${el.type}`} onMouseDown={e=>beginMove(e,el)} style={{left:s.left,top:s.top,width:s.width,height:s.height,fontSize:s['font-size'],fontWeight:s['font-weight'],fontFamily:s['font-family'],color:s.color,textAlign:s['text-align'],backgroundColor:s['background-color'],borderRadius:s['border-radius'],opacity:s.opacity,lineHeight:s['line-height'],padding:s.padding,border:s.border}}>{el.type==='text'?resolve(el.text):el.type==='image'?(el.imageUrl?<img src={resolve(el.imageUrl)} alt="Template element"/>:<span className="pdf-image-placeholder"><Image size={28}/>Choose image</span>):el.type==='link'?<a href={resolve(el.url)} onClick={e=>e.preventDefault()}>{resolve(el.text)||resolve(el.url)}</a>:el.type==='qr'?<QrPreview value={el.qrValue}/>:null}</div>})}</div></main>
  <aside className="pdf-properties">{selected?<><div className="property-title"><h3>{selected.type} element</h3><div><button title="Duplicate" onClick={()=>{const copy={...structuredClone(selected),id:uid()};mutatePage(pageIndex,p=>p.elements.push(copy));setSelectedId(copy.id)}}><Copy size={16}/></button><button title="Delete" onClick={removeElement}><Trash2 size={16}/></button></div></div>{selected.type==='text'&&<label>Content<textarea rows="4" value={selected.text||''} onChange={e=>setText(e.target.value)}/></label>}{selected.type==='image'&&<><label>Image URL or variable<input placeholder="https://... or {{customer.profileImage}}" value={selected.imageUrl||''} onChange={e=>setField('imageUrl',e.target.value)}/></label><label>Upload image<input type="file" accept="image/*" onChange={uploadElementImage}/></label></>}{selected.type==='link'&&<><label>Link label<input value={selected.text||''} onChange={e=>setText(e.target.value)}/></label><label>URL or variable<input placeholder="https://.../{{document.number}}" value={selected.url||''} onChange={e=>setField('url',e.target.value)}/></label></>}{selected.type==='qr'&&<label>QR URL / value / variable<textarea rows="3" placeholder="https://.../{{document.number}}" value={selected.qrValue||''} onChange={e=>setField('qrValue',e.target.value)}/></label>}<div className="property-grid">{['left','top','width','height','font-size'].map(p=><label key={p}>{p}<input value={style[p]||''} onChange={e=>setStyle(p,e.target.value)}/></label>)}</div><label>Text color<input type="color" value={style.color||'#111827'} onChange={e=>setStyle('color',e.target.value)}/></label><label>Background<input type="color" value={style['background-color']==='transparent'?'#ffffff':style['background-color']||'#ffffff'} onChange={e=>setStyle('background-color',e.target.value)}/></label><label>Alignment<select value={style['text-align']||'left'} onChange={e=>setStyle('text-align',e.target.value)}><option value="left">Left</option><option value="center">Center</option><option value="right">Right</option><option value="justify">Justify</option></select></label></>:<><div className="property-title"><h3>Page setup</h3></div><label>Page format<select value={page.config.format||'A4'} onChange={e=>setFormat(e.target.value)}>{Object.keys(FORMATS).map(f=><option key={f}>{f}</option>)}</select></label>{page.config.format==='CUSTOM'&&<div className="property-grid"><label>Width<input type="number" value={page.config.width} onChange={e=>updatePageConfig('width',Number(e.target.value))}/></label><label>Height<input type="number" value={page.config.height} onChange={e=>updatePageConfig('height',Number(e.target.value))}/></label></div>}<label>Page background<input type="color" value={page.config.backgroundColor||'#ffffff'} onChange={e=>updatePageConfig('backgroundColor',e.target.value)}/></label><label>Background image<input ref={fileRef} type="file" accept="image/*" onChange={uploadBackground}/></label>{page.backgroundImage&&<button className="btn btn-secondary" onClick={()=>mutatePage(pageIndex,p=>{p.backgroundImage=''})}>Remove background</button>}<label>Image fit<select value={page.bgSize||'cover'} onChange={e=>mutatePage(pageIndex,p=>{p.bgSize=e.target.value})}><option value="cover">Cover</option><option value="contain">Contain</option><option value="100% 100%">Stretch</option></select></label><label>Image position<select value={page.bgPosition||'center center'} onChange={e=>mutatePage(pageIndex,p=>{p.bgPosition=e.target.value})}><option value="center center">Center</option><option value="top center">Top</option><option value="bottom center">Bottom</option></select></label></>}</aside>
 </div>;
}
